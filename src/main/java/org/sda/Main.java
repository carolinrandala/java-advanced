package org.sda;

import org.sda.model.Person;

import java.util.function.Predicate;

public class Main {
    public static void main(String[] args) {
        //LAMBDA EXPRESSION
        //PREDICATE
       //Person person = new Person("Carolin", "Randala", 25);
       // Predicate<Person> personTest = test -> test.getAge() > 20;
       // System.out.println(personTest.test(person));
    }
}